// Header.js

// *** 패키지 import
import React from "react";

const Header = (props) => {
    return(
        <React.Fragment>
            헤더 중간 컴포넌트
        </React.Fragment>
    )
}

export default Header;